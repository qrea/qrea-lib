/// <reference path="../../typings/mocha/mocha.d.ts" />

import * as Facturation from './facturation';

describe('Hello function', () => {
  it('should return hello world', () => {
    // const result = Facturation.hello();
    // expect(result).to.equal('Hello World!');
  });
});